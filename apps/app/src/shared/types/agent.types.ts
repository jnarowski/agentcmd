/**
 * Agent Types
 *
 * Defines the supported AI agent types in the system.
 * Each agent type corresponds to a different AI CLI tool.
 */

export type AgentType = "claude" | "codex" | "cursor" | "gemini";
